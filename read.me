X - Create an array of objects to render as squares.

X - Can each be rendered as a button? - YES!

Each button should display only the title of the list

On click, toggle display to show the checklist.


BACK TO BASICS TASK LIST
 - get an event listener set up for ONE button. on 'click' -> give us a "hello world!"
 - repeat for more than one, then all buttons. 
 - this should be in a separate .js file btw...